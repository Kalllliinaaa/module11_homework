let x = 5;
let n = 2;

const power = (base, exponent) => base ** exponent;
const basa = (base, exponent) => Math.pow(base, exponent);

const multiplication = (x ,n) => console.log(power(x,n));
multiplication(x, n);